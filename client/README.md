# hcaptcha-challenge &mdash; client

This is the client code for the hCaptcha challenge.

## Running

Make sure you have the server running. Then install packages and start the dev server.

```
npm install
npm start
```
